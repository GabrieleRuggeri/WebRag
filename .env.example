# Example environment file for third-party API keys and runtime flags
# Copy this file to `.env` and fill in real secret values. Do NOT commit
# the resulting `.env` to version control. Add `.env` to your .gitignore.

# Third-party service API keys
TAVILY_API_KEY=
OPENAI_API_KEY=
LLAMA_API_KEY=

# Runtime flags
DEBUG=False

# Optional: path to a log file (used by logging configuration)
LOG_FILE=logs/app.log
